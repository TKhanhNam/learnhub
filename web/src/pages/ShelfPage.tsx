import CourseBookshelf from '../components/CourseBookshelf'

/** Kệ sách 3D — khóa học từ database. */
export default function ShelfPage() {
  return (
    <div className="page page-shelf">
      <CourseBookshelf />
    </div>
  )
}
